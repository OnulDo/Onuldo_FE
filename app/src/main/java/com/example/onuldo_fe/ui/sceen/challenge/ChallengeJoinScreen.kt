package com.example.onuldo_fe.ui.sceen.challenge

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * ③ 챌린지 참여 화면 (challenge join)
 *
 * 그림 참고 배치:
 *  - 상단: 뒤로가기(공용 OnulDoBackButton) + "챌린지 참여"
 *  - 참여할 챌린지 요약 카드
 *  - 도전 기간 선택   → component/PeriodSelector (2주 / 4주 / 8주 / 12주)
 *  - 도전 금액 선택   → component/PointSelector (10,000P …)
 *  - 유의사항 안내 박스
 *  - 요약(도전기간 / 참여금액 / 예치포인트)
 *  - 하단 "도전 시작하기" 버튼 (공용 OnulDoButton 재사용)
 *
 * TODO: UI 구현
 */
@Composable
fun ChallengeJoinScreen(
    modifier: Modifier = Modifier
) {
    // TODO: UI 구현
}
